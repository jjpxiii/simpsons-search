# Project: My TypeScript Library

## General Instructions

- When you generate new TypeScript code, follow the existing coding style.
- Ensure all new functions and classes have JSDoc comments.
- Prefer functional programming paradigms where appropriate.
- always use pnpm and not npm
- you must run `pnpm lint` `pnpm test` and `pnpm build` after every change to ensure you didn't break the code

## Coding Style

- Use 2 spaces for indentation.
- Prefix interface names with `I` (for example, `IUserService`).
- Always use strict equality (`===` and `!==`).
- Use eslint.config.mjs to check for other rules
